# Homebrew Tap

Custom Homebrew tap for Kus Andriadi's tools.

## Install

```bash
brew tap kusandriadi/tap
brew install --cask text-plus
```

## Available Casks

| Cask | Description |
|---|---|
| `text-plus` | Lightweight code editor for macOS — Notepad++ alternative |
